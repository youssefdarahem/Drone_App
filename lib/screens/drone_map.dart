import 'package:flutter/material.dart';

class DroneMap extends StatelessWidget {
  const DroneMap({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}